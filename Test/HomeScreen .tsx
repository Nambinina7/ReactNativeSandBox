import React from 'react';
import { View } from 'react-native';
import {Header} from "react-native/Libraries/NewAppScreen";

const HomeScreen = () => {
    return (
        <View>
            <Header/>
            {/* Le reste du contenu de votre écran */}
        </View>
    );
};

export default HomeScreen;
